services:
  - type: web
    name: ai-daily-license-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python telegram_license_bot.py
    autoDeploy: true
